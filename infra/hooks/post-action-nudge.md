STOP. You just changed a file. Execute this procedure before doing ANYTHING else.

1. **MEMORY** — Did you learn something non-obvious, hit a wall, make a choice that isn't self-evident from the code, or build a tool?
   - YES → Write a memory entry to `memories/entries/` NOW. Not later. NOW. Check `memories/_schema.md` for format. Deduplicate against existing entries first.
   - NO → Move to step 2.

2. **CYCLE LOG** — Append what you just did to your cycle file's "Actions Taken" section. One line. Do it now. If no cycle file exists for today, that's a problem — create one from `schedule/cycles/_template.md`.

3. **FRICTION** — Was this harder than it should have been? Could a script, tool, or shortcut eliminate the friction?
   - YES → Add to `tools/workshop/opportunities.md` with today's date. One line.

4. **NEXT** — State what follows from this change, then do it.

Do not skip these steps. Do not batch them for later. Do not say "I'll update the cycle file at the end." The procedure is the point.
